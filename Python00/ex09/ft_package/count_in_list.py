def count_in_list(lst, item):
    """Count the number of occurences of e in lst

    Args:
        lst (List[T]): list to count in
        e (T): element to count

    Returns:
        int: Number of occurences of e in lst
    """

    return lst.count(item)
